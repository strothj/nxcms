{
  "name": "@nxcms/test-fixtures",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "babel src -d dist --ignore spec.js",
    "test": "mocha --opts mocha.opts",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "mocha": "^3.4.1",
    "@nxcms/validation": "^0.1.0"
  }
}
